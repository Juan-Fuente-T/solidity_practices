// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.13;

import {Test, console2} from "lib/forge-std/src/Test.sol";
import {MiERC20} from "../src/MiERC20.sol";
/*
contract CounterTest is Test {
    MiERC20 public miERC20;
    address public alice;

    function setUp() public {
        miERC20 = new MiERC20("ii", "nj", 18);
        alice = makeAddr("alice");
        vm.deal(alice, 10 ether);
    }

    function testMint2() public {
        miERC20.mint(alice, 1 *1**18 );
        console2.log(miERC20.balanceOf(alice));
        assertEq(miERC20.balanceOf(alice), 1 *1**18);
    }

    function testFuzz_SetNumber(uint256 x) public {
        counter.setNumber(x);
        assertEq(counter.number(), x);
    }
}*/
